import { AdminSessionHelper } from './admin-session-helper';

describe('AdminSessionHelper', () => {
  it('should create an instance', () => {
    expect(new AdminSessionHelper()).toBeTruthy();
  });
});
